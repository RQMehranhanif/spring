apiVersion: apps/v1
kind: Deployment
metadata:
  name: jhooq-spring
spec:
  replicas: 5
  selector:
    matchLabels:
      app: jhooq-spring
  template:
    metadata:
      labels:
        app: jhooq-spring
    spec:
      containers:
        - name: springboot
          image: mehranhanif007/kubernetes:jhooq-k8s-spring

          resources:
            requests:
              memory: "128Mi"
              cpu: "512m"
            limits:
              memory: "128Mi"
              cpu: "512m"

          ports:
            - containerPort: 8080

          readinessProbe:
            httpGet:
              path: /hello
              port: 8080
            initialDelaySeconds: 15
            periodSeconds: 10

          livenessProbe:
            httpGet:
              path: /hello
              port: 8080
            initialDelaySeconds: 15
            periodSeconds: 10

          startupProbe:
            httpGet:
              path: /hello
              port: 8080
            failureThreshold: 30
            periodSeconds: 10

          env:
            - name: PORT
              value: "8080"
---
apiVersion: v1
kind: Service
metadata:
  name: jhooq-spring
spec:
  type: NodePort
  ports:
    - port: 80
      targetPort: 8080
  selector:
    app: jhooq-spring
